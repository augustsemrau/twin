{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Twin",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:default",
    "fs:allow-home-read",
    "fs:allow-home-write",
    "fs:allow-home-meta",
    "fs:allow-watch",
    "fs:allow-unwatch",
    "shell:default"
  ]
}
